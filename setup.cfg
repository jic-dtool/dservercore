[flake8]
exclude=venv*,env*,.tox,.git,*.egg,build,docs,migrations,jwt-spike

[tool:pytest]
testpaths = tests
#addopts = --cov=dtool_lookup_server --cov-report=term-missing --disable-warnings
addopts = --cov=dtool_lookup_server --cov-report=term-missing

[cov:run]
source = dtool_lookup_server
